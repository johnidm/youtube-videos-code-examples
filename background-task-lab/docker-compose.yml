version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - DATABASE_FILE=/app/data/sqlite.db
      - NODE_ENV=production
    volumes:
      - ./data:/app/data
    restart: unless-stopped
